import {RegisterForm} from "@/modules/auth";

const RegisterPage = () => {
    return (
        <div className="flex min-h-screen w-full items-center justify-center bg-background px-3 py-6 sm:px-4">
            <RegisterForm/>
        </div>
    );
};

export default RegisterPage;
