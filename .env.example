# Nginx proxy → backend (на сервере с host network: host.docker.internal + порт API)
BACKEND_UPSTREAM=http://host.docker.internal:5237

# Origin API для локальной разработки (не localhost в браузере)
PROD_ENV_VITE_BACKEND_ORIGIN=http://localhost:5237
