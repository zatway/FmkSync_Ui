# Upstream backend for Nginx reverse proxy
BACKEND_UPSTREAM=http://45.10.41.189:5237

# Frontend runtime backend origin (used only on localhost)
PROD_ENV_VITE_BACKEND_ORIGIN=http://localhost:5237

# API prefix appended to origin
PROD_ENV_VITE_API_PREFIX=/api/v1
